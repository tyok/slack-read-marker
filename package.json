{
  "engines": {
    "node": ">=16.0.0 < 17"
  },
  "name": "slack-read-marker",
  "version": "1.0.0",
  "description": "Mark specified channels as read, automatically",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mohammad Satrio",
  "license": "ISC",
  "dependencies": {
    "@slack/bolt": "^3.11.0",
    "@slack/web-api": "^6.7.1",
    "dotenv": "^16.0.0"
  }
}
